node_modules/
.next/
.next-prod/
out/
build/
*.log
.DS_Store
next-env.d.ts
.gstack/

# Env files (allow .env.example for documentation)
.env
.env.*
!.env.example

# Personal Claude Code config (user-specific, not project)
CLAUDE.local.md

# Editors / OS
.idea/
.vscode/
*.swp
*.swo
Thumbs.db

# Test / coverage outputs
coverage/
playwright-report/

# Dogfood-run outputs (regenerated by /dogfood-run)
public/test-report*.html
public/test-report-assets/

# typescript build info
*.tsbuildinfo

# scratch dir for skill element-spec JSON (write_element.py inputs)
tmp/

# Local user store: bcrypt-hashed passwords, session secrets, audit log.
# Server-only, never committed.
/data/

# BMAD-Method runtime artefacts under tools/bmad/. The committed source
# (_bmad/, .claude/skills/bmad-*, scripts/) is what makes the agent-builder
# reproducible; per-user state and generated outputs stay out of git.
tools/bmad/_bmad/custom/config.user.toml
tools/bmad/_bmad-output/
tools/bmad/skills/reports/
.claude/scheduled_tasks.lock
