node_modules/
.next/
.next-prod/
out/
build/
*.log
.DS_Store
next-env.d.ts
.gstack/

# Env files (allow .env.example for documentation)
.env
.env.*
!.env.example

# Personal Claude Code config (user-specific, not project)
CLAUDE.local.md

# Editors / OS
.idea/
.vscode/
*.swp
*.swo
Thumbs.db

# Test / coverage outputs
coverage/
playwright-report/

# Dogfood-run outputs (regenerated by /dogfood-run)
public/test-report*.html
public/test-report-assets/
# Throwaway dogfood/test process artifacts — not curated processes.
wiki/processes/*dogfood*
wiki/processes/dogfood-test-run-*
raw-sources/*dogfood*/
raw-sources/dogfood-test-run-*/

# Generated review/audit artifacts (e.g. the loose-ends audit report)
docs/loose-ends-audit-*.html

# Scratch mutation scripts at the repo root. These hand-edit the wiki out of
# band (forbidden by CLAUDE.md) — they must never be committed. Delete them
# once the dogfood run they drove is done.
/tmp-*.ts

# typescript build info
*.tsbuildinfo

# scratch dir for skill element-spec JSON (write_element.py inputs)
tmp/

# Local user store: bcrypt-hashed passwords, session secrets, audit log.
# Server-only, never committed.
/data/
